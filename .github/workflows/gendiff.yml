name: gendiff-check
on: push
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: snyk/actions/node@master
        with: 
          node-version: 16.x
          cache: 'npm'

      - run: make setup
      - run: make test
      - run: make lint